language: node_js
node_js:
  - "8"

branches:
  only:
    - master
    - develop

before_script:
  - npm install -g firebase-tools
  - npm install -g @angular/cli@1.2.0

script:
  - ng build --prod --output-hashing=bundles
